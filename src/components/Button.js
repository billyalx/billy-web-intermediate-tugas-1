export default function Button() {
  return (
    <section className="flex items-center justify-center w-full">
      <button className="bg-blue text-[#fff] p-[10px] font-bold text-[20px] rounded border-[3px] border-gray">
        Button
      </button>
    </section>
  );
}
